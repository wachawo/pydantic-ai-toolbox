# Runtime dependencies for pydantic-ai-toolkits with all extras.
# This mirrors pyproject.toml's [project.dependencies] + [project.optional-dependencies.all].
# `pip install -e .[all]` is the preferred path; this file is provided for
# environments that pin from a flat requirements list.

pydantic-ai-slim>=0.0.40
pydantic-ai>=0.0.40

# extra: sql
sqlalchemy>=2.0

# extra: pandas
pandas>=2.0
pyarrow>=15.0          # parquet I/O via PandasToolkit.load_parquet

# extra: rag
numpy>=1.26

# MemoryToolkit is stdlib only; no entry required.
